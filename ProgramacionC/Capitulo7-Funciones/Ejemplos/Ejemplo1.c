#include <stdio.h>
#include <stdlib.h>

int main()
{
  obtenercaracteres();    //Llamada a una funcion que obtiene los numeros
  alfabetizar();          //Llamada a la funcion que ordena alfabeticamente las letras
  verletras();            //Llamada a la funcion que visualiza letras en la pantalla
  return 0;               //retorno al sistema
}
int obtenercaracteres()
{
  /*Codigo de C para obtener una lista de caracteres*/
  return 0;               //retorno  a main()
}
int alfabetizar()
{
  /*Codigo de C para alfabetizar los caracteres */
  return (0);           //retorno a main()
}
void verletras()
{
  /*Codigo de C para visualizar lista alfabetizada*/
  return ;          //Retorno a main()
}

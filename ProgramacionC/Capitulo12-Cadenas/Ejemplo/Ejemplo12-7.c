//inicializa dos arrays de caracteres con cadenas constantes

char Cadena[81] = "C maneja eficientemente las cadenas";
char nombre[] = "Luis Martin Cebo";

/* El segundo metodo para asignacion de una cadena a otra es utilizar la funcion strcpy(). La 
funcion strcpy() copia los caracteres de la cadena fuente a la cadena de destino. La funcion supone que
la cadena destino tiene espacio suficiente para contener toda la cadena fuente. El prototipo de la funcion: */

char strcpy(char * destino, const char *fuente);
